package vpc.dart.selector;

import vpc.core.Value;
import vpc.tir.expr.Operator;

public interface InstrumentSelectorI {
	public boolean isInsteresting(Value val);
	public boolean isInsteresting(Operator op);
}
